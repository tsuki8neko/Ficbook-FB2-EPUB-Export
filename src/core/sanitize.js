/**
 * Подготавливает часть имени файла для сохранения
 *
 * Заменяет пробельные символы на подчёркивания и удаляет
 * символы, запрещённые в именах файлов большинства ОС
 */

export function sanitizeFilePart(str) {
    return str.replace(/\s+/g, "_").replace(/[\\/:*?"<>|]+/g, "");
}
